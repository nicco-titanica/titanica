// @titanica/ui — shared UI component library.
//
// The design system is governed by docs/04-DESIGN.md.
// Components are added here as the design language is defined.
// Beauty increases attention: every component should read as an institution, not a feed.

export {};
